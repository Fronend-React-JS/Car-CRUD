const nameForm = document.getElementById('name');
const yearForm = document.getElementById('year');
const priceForm = document.getElementById('price');
const colorForm = document.getElementById('color');
const addBtn = document.getElementById('add-btn');
const nameFilter = document.getElementById('nameSelect');
const yearFilter = document.getElementById('yearSelect');
const statusFilter = document.getElementById('statusSelect');
const search = document.getElementById('search');
const tbody = document.getElementById('tbody');
const form = document.getElementById('form');


export{form, nameForm, yearForm, priceForm, colorForm, addBtn, nameFilter, yearFilter, statusFilter, search, tbody}